// A client-side simplified version of the schema types 
// for use in the frontend

export interface Suggestion {
  id: number;
  name: string;
  email: string;
  suggestion: string;
  createdAt: string;
}